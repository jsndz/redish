Redis also provides a pubsub interface

client can join a channel using SUBSCRIBE command
and publish command is used to send message to it

Publishers are stateless.
no publisher data is stored 

after the subscribe command is given the subscriber mode is turned on
where select number of commands work